# 🔐 配置文件安全指南

## 为什麼需要這份指南？

标准 JSON 格式**不支持注释** (`//`)，但配置文件中的敏感信息（特別是私鑰）需要特別警告。這份文檔補充说明 `config.json` 中每个欄位的安全考量。

---

## 📋 交易所配置安全说明

### 1️⃣ Binance (CEX)

```json
{
  "exchange": "binance",
  "binance_api_key": "YOUR_API_KEY_HERE",
  "binance_secret_key": "YOUR_SECRET_KEY_HERE"
}
```

**安全等级:** 🟡 中等

**欄位说明:**
- `binance_api_key`: Binance API Key
  - ✅ 可在 Binance 後台撤銷
  - ✅ 可设定 IP 白名单
  - ✅ 可限制权限（建議只开启「交易」权限）
  
- `binance_secret_key`: Binance Secret Key
  - ⚠️ 用于簽名請求，不要洩漏
  - ✅ 洩漏後可立即在後台撤銷

**最佳實踐:**
```bash
✅ 在 Binance 後台创建专用 API Key
✅ 只开启「期货交易」权限（关闭「提現」）
✅ 绑定服务器 IP（如果固定 IP）
✅ 定期轮换 API Key（建議每月一次）
```

---

### 2️⃣ Hyperliquid (DEX)

```json
{
  "exchange": "hyperliquid",
  "hyperliquid_private_key": "your_ethereum_private_key_without_0x",
  "hyperliquid_wallet_addr": "0xYourEthereumAddress",
  "hyperliquid_testnet": false
}
```

**安全等级:** 🔴 高危

**欄位说明:**
- `hyperliquid_private_key`: **以太坊私鑰**（不是 API Key！）
  - 🔴 這是你的完整钱包私鑰
  - ❌ 洩漏 = 失去钱包完全控制權
  - ❌ 无法撤銷！唯一解決方案是创建新钱包
  - ⚠️ 必须移除 `0x` 前綴

- `hyperliquid_wallet_addr`: 以太坊钱包地址（公開地址）
  - ✅ 公開信息，可分享
  - 用于验证身份

- `hyperliquid_testnet`: 是否使用测试網
  - `false`: 主網（真實資金）
  - `true`: 测试網（测试用）

**最佳實踐:**
```bash
✅ 创建专用钱包（不要用主钱包！）
✅ 只在专用钱包存放交易所需資金（$100-500）
✅ 先在测试網测试（hyperliquid_testnet: true）
✅ 定期检查钱包交易记录
❌ 絕對不要将 config.json 提交到 Git
❌ 絕對不要重複使用你的主钱包私鑰
```

**如何创建专用钱包:**
```bash
# 方法 1: 使用 MetaMask
1. 打開 MetaMask → 账户 → 创建账户
2. 命名为 "Hyperliquid Trading Only"
3. 导出私鑰 → 填入 config.json
4. 只在這个钱包存放交易資金

# 方法 2: 使用離线工具
1. 访问 https://vanity-eth.tk (離线模式)
2. 生成新钱包
3. 妥善保管私鑰
```

---

### 3️⃣ Aster DEX (混合方案)

```json
{
  "exchange": "aster",
  "aster_user": "0xYourMainWalletAddress",
  "aster_signer": "0xAPIWalletSignerAddress",
  "aster_private_key": "api_wallet_private_key_without_0x"
}
```

**安全等级:** 🟠 中高

**欄位说明:**
- `aster_user`: 你的主钱包地址
  - ✅ 公開信息
  - 用于登入 Aster 平台

- `aster_signer`: API Wallet 地址
  - ✅ 从 Aster 网站创建
  - ✅ 可在後台禁用

- `aster_private_key`: API Wallet 私鑰
  - ⚠️ 仍然是以太坊私鑰（不是傳統 API Key）
  - ✅ 可在 Aster 後台「禁用」（阻止 API 调用）
  - ⚠️ 局限性：禁用不会改變私鑰本身
  - ⚠️ 攻擊者仍可繞過 Aster 直接操作區塊鏈

**最佳實踐:**
```bash
✅ 主钱包存放大额資金（不交易）
✅ API Wallet 只存交易金额（$100-500）
✅ 定期检查 API Wallet 权限狀態
✅ 如懷疑洩漏，立即在後台禁用
```

**创建 API Wallet:**
```bash
1. 访问 https://www.asterdex.com/en/api-wallet
2. 连接你的主钱包（MetaMask）
3. 点击 "Create API Wallet"
4. 保存三个信息：
   - Main Wallet Address (aster_user)
   - API Wallet Signer (aster_signer)
   - API Wallet Private Key (aster_private_key) ⚠️ 只显示一次！
```

---

## 🛡️ 通用安全建議

### 环境变量方式（推薦）

**为什麼用环境变量？**
- ✅ 不会意外提交到 Git
- ✅ 容易在不同环境切換
- ✅ 云端部署时更安全

**设定方式:**
```bash
# 1. 创建 .env 文件（記得加入 .gitignore）
cat > .env << 'EOL'
HYPERLIQUID_PRIVATE_KEY=your_private_key_here
BINANCE_API_KEY=your_api_key_here
BINANCE_SECRET_KEY=your_secret_key_here
EOL

# 2. 在 config.json 中使用占位符
{
  "hyperliquid_private_key": "${HYPERLIQUID_PRIVATE_KEY}"
}

# 3. 程式启动时载入环境变量
export $(cat .env | xargs)
./nofx
```

---

### 资料庫加密（強烈建議）

**目前狀態:**
- ⚠️ 私鑰以明文存在 SQLite 资料庫
- ⚠️ 任何能访问 `config.db` 的人都能读取

**启用加密:**
```bash
# 参阅 docs/ENCRYPTION_DEPLOYMENT.md
./deploy_encryption.sh
```

---

### 检查清单

在部署前，請確認：

- [ ] 所有私鑰使用专用钱包（不是主钱包）
- [ ] 专用钱包只存放必要資金
- [ ] `config.json` 已加入 `.gitignore`
- [ ] `.env` 文件已加入 `.gitignore`
- [ ] 已在测试網测试配置
- [ ] 已启用资料庫加密
- [ ] 定期检查钱包交易记录

---

## 🚨 如果私鑰洩漏了怎麼辦？

### Binance
```bash
1. 立即登入 Binance
2. API 管理 → 刪除洩漏的 API Key
3. 创建新的 API Key
4. 更新 config.json
```

### Hyperliquid
```bash
⚠️ 无法撤銷！必须：
1. 创建新的以太坊钱包
2. 将旧钱包資产轉移到新钱包
3. 更新 config.json 使用新钱包私鑰
4. 廢棄旧钱包
```

### Aster
```bash
1. 立即访问 https://www.asterdex.com/en/api-wallet
2. 禁用被洩漏的 API Wallet
3. 创建新的 API Wallet
4. 更新 config.json
5. ⚠️ 如果攻擊者已经竊取資金，无法追回
```

---

## 📖 延伸閱讀

- [README.md](README.md) - 完整配置说明
- [ENCRYPTION_DEPLOYMENT.md](docs/ENCRYPTION_DEPLOYMENT.md) - 加密系统部署
- [SECURITY.md](SECURITY.md) - 安全政策
- [Hyperliquid 官方文檔](https://hyperliquid.gitbook.io)
- [Aster API 文檔](https://docs.asterdex.com)

---

**記住：私鑰就是你的資产。保护私鑰 = 保护財产。**
